[package]
name = "temporal"
version = "0.1.0"
edition = "2021"
description = "System-under-test for the hiker worked example."

[features]
# Enable the deliberately wrong implementation to watch proptest catch it.
buggy = []

[dev-dependencies]
proptest = "1"

[lints]
workspace = true
